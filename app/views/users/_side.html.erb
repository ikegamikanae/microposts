    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1> 
    </section>
    <section class="follow_info">
      <% if logged_in? %>
        <h2><%= link_to "following", followings_user_path(@user) %></h2>
        <h2><%= link_to "followers", followers_user_path(@user) %></h2>
      <% end %>
    </section>