<div class="row">
  <aside class="col-md-4">
    <%= render 'side' %>
  </aside>
  <div class="col-md-8">
    <% if logged_in? %>
      <% if @users.any? %>
        <h3> <%= @title %> (<%= @users.count %>)</h3>
        <%= page_entries_info @users %>
        <ol class="follows">
          <%= render 'followings' %>         
        </ol>
        <%= paginate @users %>
      <% end %>
    <% end %>
  </div>